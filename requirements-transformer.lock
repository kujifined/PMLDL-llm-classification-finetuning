# Reproducible transformer stack for E2 and the competitive Gemma track.
#
# Install the baseline first, then this file:
#   python -m pip install -r requirements-baseline.lock
#   python -m pip install -r requirements-transformer.lock
#
# Kaggle CUDA notebooks may keep the platform-provided torch build when it is
# already 2.6.0; every run still records `pip freeze`, CUDA and GPU metadata.
torch==2.6.0
transformers==4.57.6
peft==0.18.1
accelerate==1.12.0
bitsandbytes==0.49.2
sentencepiece==0.2.1
protobuf==6.33.6
safetensors==0.7.0

# Managed-notebook execution and experiment tracking.
clearml==2.1.12
nbclient==0.11.0
nbformat==5.11.1
